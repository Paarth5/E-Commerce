<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>E-Commerce</title>
  </head>
  <body>
    <h1>Edit Review</h1>
    <form
      action="/products/<%= item._id %>/review/<%= review._id %>?_method=PUT"
      method="POST"
    >
      <label for="username">UserName*: </label>
      <input
        type="text"
        name="review[username]"
        id="username"
        value="<%=review.username %>"
        disabled
      />
      <label for="rating">Rating*: </label>
      <input
        type="range"
        name="review[rating]"
        id="rating"
        min="1"
        max="5"
        step="1"
        value="<%=review.rating %>"
        required
      />
      <label for="description">Description: </label>
      <textarea name="review[description]" id="description" cols="50" rows="3">
<%= review.description %></textarea
      >
      <button>Edit Review</button>
    </form>
    <a href="/products/<%= item._id %>"><button>Cancel</button></a>
  </body>
</html>
